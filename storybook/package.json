{
  "name": "prism-storybook",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "storybook:default": "npm run storybook --workspace=packages/ds-default",
    "build:default": "npm run build-storybook --workspace=packages/ds-default",
    "install:all": "npm install"
  }
}
